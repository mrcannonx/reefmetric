node_modules/
dist/
.astro/
.DS_Store
*.log
.env
.env.*

# internal strategy — keep out of the public repo
docs/

# Reddit growth playbook — NEVER commit (public repo; discoverable promo coordination)
reddit/
